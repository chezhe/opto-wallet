export const walletInject = `
  
`

// window.onmessage = function (event) {
//   if (event.data) {
//     const data = JSON.parse(event.data)
//     if (data.event === 'login') {
//       window.localStorage.setItem(
//         data.authKey + '_wallet_auth_key',
//         JSON.stringify(data.value)
//       )
//     }
//   }
// }
